# Please set these variables. They are used in execution time.
#
# the run command consists
# > $RUNCMD $RUNOPT $NCOREOPT $NCORE[...] $NPROCOPT $NPROC[...]  executable
# 
# where 
#     [...]    for run
#     LAT      Latency
#     BDTH     Bandwidth
#     PP       PingPong
#     BCAST    Broadcast
#     RDC      Reduce
#     PART     Partial data
#
# For example:
# 1. use these settings: (for TOTAL Houston HPC environment)
#   RUNCMD=autompirunB
#   RUNOPT=-nopam
#   NCOREOPT=-nc
#   NPROCOPT=-np
# implies the command
# > autompirunB -nopam -nc 1 -np 2 .../ping_pong .....

# 2. use these settings: (for generic mpirun environment)
   RUNCMD=cafrun
#   RUNOPT=-hostfile myhostfile
#   NCOREOPT= -n 
   NPROCOPT=-n
# implies the command
# > mpirun -hostfile myhostfile -np 2 .../ping_pong .....

# 3. use these settings: (for SLURM environment)
#   JOBCMD=salloc
#   NCOREOPT=-N
#   JOBOPT=-p ... --exclusive
#   RUNCMD=mpirun
#   NPROCOPT=-n
#   RUNOPT=--image-heap=1G -v
#   RUNOPT2=--loadbalance
# implies the command
# > salloc -n ... -N .. -p ... 


# Latency 
NCORELAT=2
NPROCLAT=2

# Bandwidth 
NCOREBW=2
NPROCBW=2

# PingPong
NCOREPP=2
NPROCPP=2

# Bidirectional
NCOREBD=2
NPROCBD=2

# Broadcast
NCOREBCST=8
NPROCBCST=8

# Noncontiguous
NCORENC=2
NPROCNC=2

